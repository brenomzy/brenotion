# Copy this file to .env only when a spike or approved adapter requires it.
# Never commit real values. Candidate integrations remain subject to validation.

# Public client configuration (not a secret)
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
EXPO_PUBLIC_CONVEX_URL=

# Backend-only secrets and deployment identifiers
CLERK_SECRET_KEY=
CONVEX_DEPLOYMENT=
CLERK_JWT_ISSUER_DOMAIN=
AUTHORIZED_CLERK_USER_ID=
OPENAI_API_KEY=
